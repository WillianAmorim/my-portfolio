import { BrowserRouter as Router, Switch, Route } from "react-router-dom";

const Routes = () => {
  <Router>
    <Switch>
      <Route exact path="/" />
    </Switch>
  </Router>
}

export default Routes